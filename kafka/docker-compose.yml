services:
  broker:
    #build:
      #context: .
    image: apache/kafka-native:3.9.0
    container_name: kafka_broker_0
    ports:
      - 9092:9092
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093,LISTENER_KOWL://:9094
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092,LISTENER_KOWL://kafka_broker_0:9094
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,LISTENER_KOWL:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_NUM_PARTITIONS: 1
    networks:
      - my_network
    volumes:
      - ./kafka_data/data:/var/lib/kafka/data
      - ./kafka_data/log:/tmp/kafka-logs
      - ./kafka_data/config:/opt/kafka/config
    restart: always

volumes:
  kafka_data:

networks:
  my_network:
    name: my_network
    driver: bridge
